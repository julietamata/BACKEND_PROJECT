<div>
<h3> Mi carrito {{ cid}}</h3>
</div>

<table class="table table-dark table-striped">
  <thead>
    <tr>
      {{!-- <th scope="col">ID</th> --}}
      <th scope="col">Producto</th>
      <th scope="col">Cantidad</th>
      <th scope="col">Precio</th>
      
      <th scope="col">Stock</th>

      <th scope="col">Imágenes</th> 
      <th scope="col">Eliminar</th> 
    </tr>
  </thead>
  <tbody>
    {{#each products}}
    <tr>
      {{!-- <th scope="row">{{this.id}}</th> --}}
      <td>{{this.product.title}}</td>
      <td>{{this.quantity}}</td>
      <td>{{this.product.price}}</td>
      
      <td>{{this.product.stock}}</td>
       
      <td><img src="{{this.product.thumbnail}}" alt=""></td>
    <td> <a class="btn btn-danger" href="#" role="button"><img src="https://cdn.icon-icons.com/icons2/868/PNG/512/trash_bin_icon-icons.com_67981.png" width="20" alt=""> </a> </td>
    </tr>


    {{/each}}

    </tbody>

    </table>
